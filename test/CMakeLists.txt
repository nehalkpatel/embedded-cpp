# System-level tests.
#
# Currently empty: end-to-end coverage lives in py/host-emulator/tests/ and runs
# via the `host_emulator_test` CTest target. This directory is a placeholder for
# system tests that don't fit the Python emulator harness.
